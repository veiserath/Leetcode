#942 leetcode
import random
def didStringMatch(S):
	for letter, number in map(S, C):
		if letter = 'I':
			for number in C:
				
		if letter = 'D':
	A = []
	B = len(S) + 1
	C = list(range(B))
	return C
S = 'IDID'
print(diStringMatch(S))

